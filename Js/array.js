let names = ['Kinley', 'Pema', 'Wangmo'];

names[3] = 'Zangmo';  //one way *spec
// names.push('Jordan');    // push *add
// names.unshift('Zangmo'); //add at the end
// names.pop();             //will remove last array,
// names.shift('Bangmo');

// console.log(names.lenght);
console.log(names[3]);